# application.properties (para desarrollo o producción)

# Configuración para MySQL (en producción o desarrollo)
spring.datasource.url=jdbc:mysql://localhost:3306/edutechbbdd
spring.datasource.username=root
spring.datasource.password=elefante2024
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# Configuración activa para el perfil de desarrollo
spring.profiles.active=dev

# Configuración para Swagger
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/doc
